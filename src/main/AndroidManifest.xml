<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ghifari.pptb">

    <application
        android:allowBackup="true"
        android:icon="@drawable/e"
        android:label="NutriAssist"
        android:roundIcon="@drawable/e"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity"></activity>
        <activity android:name=".KaloriActivity"></activity>
        <activity android:name=".PenyakitActivity"></activity>
        <activity android:name=".MakananActivity"></activity>
        <activity android:name=".Makanan2Activity"></activity>
        <activity android:name=".AkhirActivity"></activity>
        <activity android:name=".DisplayActivity"></activity>
    </application>

</manifest>